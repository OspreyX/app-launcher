'use strict';

describe('Service: subscriptionManager', function () {

  // load the service's module
  beforeEach(module('appLauncherApp'));

  // instantiate service
  var subscriptionManager;
  beforeEach(inject(function (_subscriptionManager_) {
    subscriptionManager = _subscriptionManager_;
  }));

  // it('should do something', function () {
  //   expect(!!subscriptionManager).toBe(true);
  // });

});
