'use strict';

describe('Service: appHeight', function () {

  // load the service's module
  beforeEach(module('appLauncherApp'));

  // instantiate service
  var appHeight;
  beforeEach(inject(function (_appHeight_) {
    appHeight = _appHeight_;
  }));

  // it('should do something', function () {
  //   expect(!!appHeight).toBe(true);
  // });

});
